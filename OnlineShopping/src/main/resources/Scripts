*******************USER TABLE****************************

CREATE database ONLINESHOPPINGDB;

use ONLINESHOPPINGDB;
show tables;


create table USER (
ID int auto_increment,
FULL_NAME varchar(50) NOT null,
MAIL_ID varchar(50) unique,
PASSWORD varchar(20) NOT NULL,
MOBILE_NUMBER varchar(10) unique NOT NULL,
primary key (ID));

SELECT * FROM USER;

INSERT INTO USER(FULL_NAME, MAIL_ID, PASSWORD, MOBILE_NUMBER) VALUES
('Govind', 'gk@gmail.com', 'gk1234', 9822227777);

create table PRODUCT (
PRODUCT_ID INT AUTO_INCREMENT,
PRODUCT_BRAND varchar(50) NOT NULL,
PRODUCT_TYPE VARCHAR(50) NOT NULL,
PRICE DECIMAL(10) NOT NULL,
CATEGORY varchar(10),
primary key (PRODUCT_ID));

ALTER TABLE PRODUCT AUTO_INCREMENT=1000;

INSERT INTO PRODUCT(PRODUCT_BRAND,PRODUCT_TYPE, PRICE,CATEGORY) VALUES
('Nike','T-Shirt', 500,'Men');
INSERT INTO PRODUCT(PRODUCT_BRAND,PRODUCT_TYPE, PRICE,CATEGORY) VALUES
('Manyavar','Saree', 1000,'Women');
INSERT INTO PRODUCT(PRODUCT_BRAND,PRODUCT_TYPE, PRICE,CATEGORY) VALUES
('Manyavar','Saree', 1000,'Women');
INSERT INTO PRODUCT(PRODUCT_BRAND,PRODUCT_TYPE, PRICE,CATEGORY) VALUES
('Tommy Hilfiger','Jeans', 1200,'Kids');
INSERT INTO PRODUCT(PRODUCT_BRAND,PRODUCT_TYPE, PRICE,CATEGORY) VALUES
('Leather Lounger','Sofa Covers', 950,'Home');

delete from PRODUCT where PRODUCT_ID=100;

select * from Product;
